# ================================================================
# requirements-dev.txt — Development & CI tools
# ================================================================
# Install with: pip install -r requirements-dev.txt
#
# These are NOT needed in production (Docker containers, Airflow workers).
# Runtime dependencies are in requirements.txt.
# ================================================================

# ── Linting ──
sqlfluff>=3.0.0
sqlfluff-templater-dbt>=3.0.0
flake8>=7.0.0

# ── Testing ──
pytest>=8.0.0
pytest-cov>=5.0.0
